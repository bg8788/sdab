name: Deploy to Firebase Hosting (multi-site)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        site: ['sadab-ali-7', 'sadabali7777']
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          if [ -f package.json ]; then
            npm ci
          fi

      - name: Build project
        run: |
          if [ -f package.json ]; then
            npm run build 2>/dev/null || echo "No build script found, deploying as-is"
          fi

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Create Firebase config
        run: |
          echo '{"projects":{"default":"sadabali-5214b"}}' > .firebaserc
          cat > firebase.json <<'JSON'
          {
            "hosting": {
              "site": "${{ matrix.site }}",
              "public": ".",
              "ignore": [
                "firebase.json",
                "**/.*",
                "**/node_modules/**",
                "**/*.log",
                "README.md",
                "package*.json"
              ],
              "rewrites": [
                {
                  "source": "**",
                  "destination": "/index.html"
                }
              ]
            }
          }
          JSON

      - name: Deploy to Firebase (${{ matrix.site }})
        run: |
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > serviceAccount.json
          export GOOGLE_APPLICATION_CREDENTIALS=serviceAccount.json
          firebase deploy --only hosting:${{ matrix.site }} --project sadabali-5214b --non-interactive
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
